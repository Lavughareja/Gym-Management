# ─────────────────────────────────────────────────────────────────────────────
# Vite environment variables — prefix with VITE_ to expose to the browser
# ─────────────────────────────────────────────────────────────────────────────

# Razorpay publishable/public key (get from Razorpay Dashboard → API Keys)
VITE_RAZORPAY_KEY_ID=rzp_test_StgMX0peQZajFc

# API Base URLs — controlled by IS_LOCAL / IS_LIVE flags in constant.ts
# (These are just for reference; the actual switch happens in constant.ts)
# VITE_API_LOCAL=http://localhost:5000/api
# VITE_API_STAGING=https://staging-api.gymmanagement.com/api
# VITE_API_LIVE=https://api.gymmanagement.com/api
